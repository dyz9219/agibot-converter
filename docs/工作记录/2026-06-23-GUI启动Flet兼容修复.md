# GUI 启动 Flet 兼容修复

## 问题背景

用户反馈远程下载的 `DataConverterShell-full.exe` 双击无法打开，截图报错为：

```text
module 'flet.controls.padding' has no attribute 'symmetric'
```

此前验证只覆盖了打包 exe 的 `--internal-run-conversion` 内部转换入口，未覆盖 GUI 双击启动链路，因此对 Windows 包可用性的结论不完整。

## 根因分析

本地开发环境的 Flet 仍支持 `ft.padding.symmetric(...)` / `ft.padding.only(...)` 这类模块级 helper。
远程 Windows full 包中打入的 Flet 版本为 class-based API：`flet.controls.padding` 模块只导出 `Padding` 类，
`symmetric` / `only` 是 `ft.Padding` 的类方法，不再是 `ft.padding` 模块函数。

进一步检查发现 `border` 也存在同类差异：远程包中的 `flet.controls.border` 只导出 `Border` 类，
`all` / `only` 是 `ft.Border` 类方法。若只修复 padding，GUI 启动后仍可能继续在 `ft.border.all(...)`
或 `ft.border.only(...)` 处失败。

## 改动方案

在 `src/data_converter/main.py` 增加兼容 helper：

- `_padding_symmetric(...)`
- `_padding_only(...)`
- `_border_all(...)`
- `_border_only(...)`

这些 helper 优先使用旧版模块级 API；若不存在，则回退到新版 `ft.Padding` / `ft.Border` 类方法。
同时将 GUI 构建路径中的 `ft.padding.symmetric/only` 与 `ft.border.all/only` 调用统一替换为兼容 helper。

## 回归测试

在 `test/python/test_main_entry.py` 中新增两个回归测试，使用 fake Flet 对象模拟远程包中的 class-based API：

- `test_padding_helpers_support_class_based_flet_padding_api`
- `test_border_helpers_support_class_based_flet_border_api`

先验证测试在 helper 不存在时失败，再实现兼容层后转绿。

## 验证结果

源码测试：

```powershell
pytest -q test\python\test_main_entry.py -k "padding or border"
```

结果：

```text
2 passed, 2 deselected in 0.53s
```

完整测试：

```powershell
pytest -q test\python
```

结果：

```text
58 passed, 6 skipped in 17.28s
```

## 当前结论与下一步

源码层已修复 Flet padding/border 新旧 API 差异，并通过自动化回归测试。
但这还不等价于 Windows exe 双击 GUI 已验证通过；下一步必须提交推送，等待 GitHub Actions 重新生成 Windows full 包，
下载新 artifact 后用新 `DataConverterShell-full.exe` 验证 GUI 启动，再复验真实 zip 的 LeRobot 转换结果。

## 作废的旧远程包复验

以下旧验证已按用户要求作废，不作为结论依据：

```text
run=27997530805
commit=216499a29895c2088fcb4cf48f54632fcba9e816
local_dir=test\remote-package-verify-216499a
```

本地旧验证产物已删除；`test\remote-package-verify-216499a` 只剩一个 0 文件的空目录壳，后续不再使用。

## Fresh 远程 Windows full 包复验

按用户要求重新提交并触发 GitHub Actions：

```text
692c407 chore: trigger fresh windows package verification
```

GitHub Actions run `28000315770` 完成，`build-windows` job 成功：

- `Build full package`: success
- `Verify packaged any4`: success
- `Verify build fingerprint`: success
- `Upload artifact`: success

artifact 信息：

```text
name=DataConverterShell-Windows-full
artifact_id=7810909332
size=455942766
```

下载 zip：

```text
test\remote-package-verify-692c407-fresh\DataConverterShell-Windows-full.zip
size=455942766
```

从该 zip 重新解压到：

```text
test\remote-package-verify-692c407-fresh\from-zip\
```

包内 exe 构建信息：

```json
{"profile":"full","git_commit":"692c407962094bc5fb9c2e5f41183679bf74fb77","git_dirty":false}
```

GUI 启动 smoke：

```powershell
Start-Process test\remote-package-verify-692c407-fresh\from-zip\DataConverterShell-full.exe
```

观察 15 秒后进程仍在运行，未复现 `flet.controls.padding` / `flet.controls.border` 启动崩溃。

真实 zip 转换复验：

```text
input=E:\Users\dyz\Documents\WXWork\1688858286666779\Cache\File\2026-06\task_2059925964389343234.zip
output=test\remote-package-verify-692c407-fresh\from-zip-conversion-output\task_2059925964389343234__lerobot_v30
```

为避免 PowerShell 控制台编码干扰，验证输出使用 `ensure_ascii=True`：

```json
{
  "manifest_status": "success",
  "runtime_mode": "bundled",
  "task_value": "task_2059925964389343234 | \u628a\u74f6\u5b50\u653e\u8fdb\u7b50\u5b50\u91cc",
  "episode_arrow": "task_2059925964389343234 | \u628a\u74f6\u5b50\u653e\u8fdb\u7b50\u5b50\u91cc",
  "episode_pandas": "task_2059925964389343234 | \u628a\u74f6\u5b50\u653e\u8fdb\u7b50\u5b50\u91cc",
  "expected": "task_2059925964389343234 | \u628a\u74f6\u5b50\u653e\u8fdb\u7b50\u5b50\u91cc",
  "all_expected": true,
  "has_placeholder": false
}
```

当前结论：本轮 fresh Windows full zip 已通过 GUI 启动 smoke 与真实 zip 转换复验；`annotation_result.json[action_text]`
已进入 LeRobot metadata，没有退化为 `auto-adapted scene`。

## 依赖打包冗余审计

### 问题背景

用户要求检查转换器依赖包是否把无关紧要的依赖也打进去了，并覆盖整个项目。

### 检查范围

本轮检查覆盖：

- `pyproject.toml`
- `scripts/install_curated_env.ps1`
- `scripts/build_exe.ps1`
- `scripts/build_exe_onefile.ps1`
- `scripts/smoke_lerobot_exe.ps1`
- `DataConverterShell-*.spec`
- `src/data_converter/`
- `any4lerobot/`
- 当前旧产物 `dist/DataConverterShell/`

### 现象与量化结果

当前仓库中没有 `dist/DataConverterShell-full/` 或 `dist/DataConverterShell-fast/`，但存在旧产物：

```text
dist/DataConverterShell/_internal = 1735.56 MB
dist/DataConverterShell-Windows.zip = 499.14 MB
```

旧产物 `_internal` 体积排名前列：

```text
torch             1064.53 MB
cv2                 97.92 MB
flet_desktop        94.69 MB
imageio_ffmpeg      83.58 MB
av.libs             80.80 MB
ray                 77.06 MB
pyarrow             74.57 MB
numpy.libs          36.40 MB
PIL                 12.70 MB
pandas              12.54 MB
```

进一步拆分：

```text
torch/lib         1005.45 MB
ray/dashboard        9.38 MB
ray/rllib            6.04 MB
ray/data             3.13 MB
```

### 根因分析

包体膨胀主要不是 `any4lerobot` 源码本身导致，`any4lerobot` 目录约 0.41 MB；主要来自 PyInstaller 的整包收集策略。

`full`/`onefile`/smoke 打包链路均使用了多处 `--collect-all`：

```text
flet
flet_desktop
ray
torch
lerobot
agibot_utils
psutil
rosbags
```

其中 `torch` 的 Windows wheel 自带大量动态库，单项超过 1GB，是第一优先级。`ray` 使用 `collect-all` 会额外拉入 dashboard、rllib、data、serve、autoscaler、tune、train、llm 等本项目未直接使用的子模块。

`scripts/install_curated_env.ps1` 还额外安装了不在 `pyproject.toml` 中的包：

```text
diffusers
accelerate
cmake
einops
av
pynput
pyserial
wandb
```

旧产物中未发现 `diffusers`、`accelerate`、`cmake`、`einops`、`pynput`、`serial`、`wandb`、`tensorflow`、`tensorflow_datasets`、`datatrove`、`libero`、`robosuite`、`apache_beam` 目录，因此它们在当前旧包里不是主要体积来源。但这些包留在 curated 环境里会增加 PyInstaller 误收集风险、安装耗时和环境复杂度。

### 依赖分层结论

可明确保留：

- `flet` / `flet_desktop`：GUI 必需。
- `h5py` / `numpy` / `opencv-python-headless` / `pillow`：raw 适配、视频/图像处理、Rosbag 转换必需。
- `rosbags`：Rosbag 输出必需。
- `pyarrow` / `jsonlines` / `datasets`：LeRobot v3.0/v2.1/v2.0 元数据和 parquet 后处理仍在使用。
- `lerobot`：`any4lerobot/agibot2lerobot/agibot_h5.py` 顶层依赖 `LeRobotDataset`。
- `av`：`agibot_utils/lerobot_utils.py` 顶层导入，且视频编码链路依赖它或其动态库。

可优先裁剪或降级收集：

- `ray[default]`：项目只需要本地 `ray.init` / `ray.remote` / `ray.get` / `RuntimeEnv`，不需要 dashboard、rllib、serve、autoscaler、tune、train、llm。应优先尝试将安装依赖从 `ray[default]` 改为 `ray`，并把 PyInstaller 从 `--collect-all ray` 改为最小 hidden-import/data 收集。
- `torch`：当前 `agibot_h5.py` 通过懒加载仅用于识别/转换 torch tensor，但 `lerobot` 本身可能间接依赖。可作为高收益专项验证：先移除 `--collect-all torch`，让 PyInstaller 只收分析到的必要二进制；若 LeRobot 运行缺库，再逐项补 hidden-import/binaries。
- `lerobot`：不需要整包策略里的 `policies`、`robots`、`teleoperators`、`rl`、`scripts` 等训练/机器人控制模块。可尝试只收集 `lerobot.datasets`、`lerobot.common.datasets`、`lerobot.utils` 相关模块。
- `tkinter`：主应用是 Flet，源码未直接导入 tkinter。若 Flet Desktop 不要求，可删除 `--collect-data tkinter` 做 smoke 验证。

可从 curated 环境先移除：

- `diffusers`
- `accelerate`
- `cmake`
- `einops`
- `pynput`
- `pyserial`
- `wandb`

这些包在源码静态导入中没有对应业务路径；当前旧产物也未打入对应顶层目录。

暂不建议删除：

- `torchvision`：虽然 AgiBot 当前主路径未直接导入，但 `robomind2lerobot` 工具链中有顶层导入；如果 bundled any4 仍保留全部 upstream 工具目录，贸然删会影响健康检查或未来入口。
- `pandas`：由 `datasets`/版本转换工具链间接使用，旧产物中体积不大，风险收益比不高。
- `huggingface-hub`：版本转换脚本中有导入，且 `lerobot` 生态常依赖。

### 改动建议

本轮仅做审计，未改打包脚本。建议后续分两阶段做：

1. 低风险清理：从 `install_curated_env.ps1` 移除无业务导入的额外包，并确认 `pyproject.toml` 与 curated 环境一致。
2. 打包瘦身专项：优先处理 `ray` 与 `torch` 的 `collect-all`，每次只改一类依赖，重新 `full` 打包并执行依赖校验与真实转换 smoke。

### 验证方式

后续每轮瘦身后至少执行：

```powershell
.\scripts\build_exe.ps1 -Profile full -Clean
.\scripts\verify_packaged_any4.ps1 -DistRoot dist/DataConverterShell-full
pytest -q
```

并用干净路径做最小真实转换 smoke，确认：

- GUI 能启动；
- `--internal-build-info` 可读取；
- LeRobot v3.0 转换成功；
- v2.1/v2.0 后处理路径不缺 `pyarrow`、`datasets`、`jsonlines`；
- Rosbag health 和最小 Rosbag 转换不缺 `rosbags` 类型库。

### 当前结论

确实存在可以优化的冗余依赖和过宽收集策略。当前最大问题是 `torch` 和 `ray` 被整包收集；其次是 curated 安装脚本包含多项当前业务未使用的依赖。保守预计，仅优化 `ray` 的安装 extras 与收集范围可以减少几十 MB；若能安全减少 `torch` 整包收集，收益可能达到数百 MB 到 1GB 级别，但需要完整转换 smoke 验证。

### torch 必要性补充确认

用户追问 `torch` 是否必要。本轮复查结果：

- `lerobot==0.4.4` 的包元数据声明依赖 `torch<2.11.0,>=2.2.1`。
- `torchvision==0.17.2` 的包元数据声明依赖 `torch==2.2.2`。
- `any4lerobot/agibot2lerobot/agibot_h5.py` 中通过 `importlib.import_module("torch")` 懒加载 `torch`，用于判断/处理 torch tensor。
- `any4lerobot/ds_version_convert/v16_to_v20/convert_dataset_v16_to_v20.py` 也直接导入并使用 `torch`，但当前主业务路径主要是 AgiBot -> LeRobot v3.0/v2.1/v2.0，不一定会执行 v16_to_v20。

当前判断：`torch` 对 bundled LeRobot 转换运行时是必要依赖，不能简单删除；但 `--collect-all torch` 是否必要仍需专项验证。优化方向应是保留 `torch` 依赖，同时尝试减少 PyInstaller 对 `torch` 的全量收集范围。

## 依赖瘦身实施与本地验证

### 问题背景

用户要求实际修改依赖和打包配置，移除不必要依赖；本地验证转换通过后再推送 GitHub，并下载远程 zip 复验。同时清理历史测试产物，避免继续占用 D 盘。

### 改动方案

本轮采用低风险瘦身：

- 将 `ray[default]>=2.9` 改为 `ray>=2.9`，避免安装 dashboard/rllib/serve/tune/train 等默认额外依赖。
- 将 PyInstaller 中的 `--collect-all ray` 改为 `--collect-submodules ray` + `--collect-binaries ray`，不再收集 Ray 全量 data。
- 从 curated 环境移除并主动卸载：
  - `diffusers`
  - `cmake`
  - `einops`
  - `pynput`
  - `pyserial`
  - `wandb`
- 保留/恢复 `accelerate`：第一次移除后，本地 exe 真实转换预检失败，错误为 `ModuleNotFoundError: No module named 'accelerate'`，确认它是当前 bundled any4/LeRobot 导入链路必需依赖。
- 新增 `test/python/test_packaging_dependencies.py`，防止后续把已裁剪依赖重新加入安装规格，或重新使用 `collect-all ray`。

### 清理内容

已清理仓库内历史测试和打包产物：

- 旧 `dist/DataConverterShell` 与 `dist/DataConverterShell-Windows.zip`
- 旧 `build/DataConverterShell-full`
- 旧远程包复验目录 `test/remote-package-verify-692c407-fresh`
- 未跟踪历史测试产物目录：`test/smoke`、`test/perf`、`test/packaging`、`test/agibot_test`、`test/test_case`
- 旧临时目录：`.tmp-*`、`out_*`、`smoke-runs`
- 旧 GitHub Actions 日志 html/json/zip 与解压目录

清理后 `test/` 下仅保留正式测试代码目录 `test/python`。

### 本地验证

源码测试：

```powershell
pytest -q test\python
```

结果：

```text
61 passed, 6 skipped in 16.34s
```

full 打包：

```powershell
.\scripts\build_exe.ps1 -Profile full -Clean
```

结果：成功生成：

```text
dist\DataConverterShell-full\DataConverterShell-full.exe
```

包体量化：

```text
dist\DataConverterShell-full\_internal = 1591.27 MB
ray = 16.81 MB
```

对比旧产物：

```text
旧 dist\DataConverterShell\_internal = 1735.56 MB
旧 ray = 77.06 MB
```

本轮减少约 `144.29 MB`；Ray 目录减少约 `60.25 MB`。

裁剪依赖检查：

```text
diffusers: absent
cmake: absent
einops: absent
pynput: absent
serial: absent
wandb: absent
```

any4 一致性：

```powershell
.\scripts\verify_packaged_any4.ps1 -DistRoot dist/DataConverterShell-full
```

结果：

```text
OK: packaged any4 matches repo source.
sha256=46F8E4D4D585F79C203E3030D418ACFCC4238FAB52ED6CB5B77A3A8E8F03B28E
```

bundled any4 健康检查：

```powershell
DataConverterShell-full.exe --internal-run-any4-health --version v3.0
```

结果：

```text
ANY4_HEALTH_OK
mode=bundled
```

真实 zip 转换：

```powershell
DataConverterShell-full.exe --internal-run-conversion `
  --input-path E:\Users\dyz\Documents\WXWork\1688858286666779\Cache\File\2026-06\task_2059925964389343234.zip `
  --output-path test\local-slim-verify\from-local-exe `
  --target lerobot `
  --version v3.0 `
  --fps 30 `
  --concurrency 4
```

结果：

```text
RUN_SUMMARY total=1 success=1 failed=0 skipped=0
```

输出核验：

```json
{
  "manifest_status": "success",
  "codebase_version": "v3.0",
  "parquet_count": 3,
  "episode_rows": 460,
  "task_text": "把瓶子放进筐子里"
}
```

GUI 启动 smoke：

```text
Start-Process dist\DataConverterShell-full\DataConverterShell-full.exe
```

观察 15 秒后进程仍在运行：

```text
gui_alive_after_15s=True
```

### 当前结论

本地源码测试、full 打包、any4 bundled 健康检查、真实 zip 转换、GUI 启动 smoke 均已通过。`accelerate` 已确认为必要依赖；`diffusers/cmake/einops/pynput/pyserial/wandb` 在当前 AgiBot -> LeRobot v3.0 转换链路中可裁剪。下一步推送 GitHub，等待远程 Windows full artifact，再下载 zip 进行同样的 GUI 和真实转换复验。
